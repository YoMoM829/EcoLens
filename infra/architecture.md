# Architecture diagram

The diagram below reflects the implemented AWS and OCI split: CloudFront serves the React/Vite static frontend from S3, Cognito handles authentication, API Gateway HTTP API v2 fronts the Lambda API, S3 event processing and ML inference run inside the ML processor Lambda (PyTorch and ml_pipeline bundled into a container image via ECR), and OCI hosts only the NoSQL metadata database.

**Access model**: EcoLens is a shared platform. Authentication (Cognito JWT) is required on every API call. All files are searchable and viewable by any registered user. Tag edits are also open to all authenticated users. **Deletion is owner-only** — `DELETE /media` enforces an ownership check and returns a `forbidden` list for files uploaded by other users. SNS notifications are delivered to all subscribers whose species watch-list matches the tags on any newly uploaded file, regardless of who uploaded it.

```mermaid
---
id: a702409f-25c2-4228-80f3-235271adde4a
---
flowchart LR
  classDef user fill:#ffffff,stroke:#333,stroke-width:1px;
  classDef fe fill:#e0f7fa,stroke:#0288d1,stroke-width:1px;
  classDef aws fill:#e3f2fd,stroke:#1565c0,stroke-width:1px;
  classDef oci fill:#ffebee,stroke:#c62828,stroke-width:1px;
  classDef notif fill:#f3e5f5,stroke:#6a1b9a,stroke-width:1px;
  classDef sec fill:#eeeeee,stroke:#424242,stroke-width:1px,stroke-dasharray:3 3;

  U["User"]:::user
  B["Browser (React/Vite SPA)"]:::user
  U --> B

  subgraph FE["Frontend (S3 + CloudFront)"]
    CF["CloudFront distribution"]:::fe
    S3_FE["S3 bucket: frontend static assets"]:::fe
  end

  subgraph AWS["AWS Cloud"]
    COG["Cognito User Pool"]:::aws
    APIGW["API Gateway HTTP API v2"]:::aws
    L_API["Lambda: API handler (FastAPI/Mangum, ZIP package, 1024 MB)"]:::aws
    S3_UP["S3: uploads (originals — presigned PUT, ObjectCreated trigger)"]:::aws
    S3_TH["S3: thumbnails (generated by ML Lambda)"]:::aws
    S3_DT["S3: detections (MegaDetector JSON)"]:::aws
    S3_MD["S3: models (MegaDetector + classifier weights)"]:::aws
    S3_QT["S3: query-temp (ephemeral reference files — deleted after query)"]:::aws
    L_PIPE["Lambda: media processor (S3 event, container image via ECR, 3008 MB)"]:::aws
    SNS["SNS topic: tag notifications"]:::notif
    CW["CloudWatch logs and metrics"]:::aws
  end

  subgraph OCI["Oracle Cloud Infrastructure"]
    NOSQL["Oracle NoSQL DB: media metadata + tags"]:::oci
  end

  subgraph SEC["Security and config"]
    IAM["IAM roles and bucket policies"]:::sec
    SECRETS["Secrets and env vars (incl. OCI keys)"]:::sec
  end

  B --> CF
  CF --> S3_FE

  B -->|"Sign up / sign in"| COG
  B -->|"Call protected API with Cognito JWT"| APIGW

  APIGW --> L_API
  L_API -->|"Validate JWT / JWKS"| COG
  L_API -->|"Presign uploads"| S3_UP
  L_API -->|"Presign query-temp / invoke ML"| S3_QT
  L_API -->|"Search, list, admin"| NOSQL

  B -->|"Upload via presigned URL"| S3_UP
  S3_UP -->|"ObjectCreated event"| L_PIPE

  L_PIPE -->|"Compute checksum / deduplicate"| CW
  L_PIPE -->|"Read media"| S3_UP
  L_PIPE -->|"Write thumbnails"| S3_TH
  L_PIPE -->|"Write detection JSON"| S3_DT
  L_PIPE -->|"Download models at cold start"| S3_MD
  L_PIPE -->|"Write metadata and tags"| NOSQL
  L_PIPE -->|"Publish tag notifications"| SNS

  SNS -->|"Email notifications"| U

  L_API --> CW
  L_PIPE --> CW

  IAM -. controls .- CF
  IAM -. controls .- S3_FE
  IAM -. controls .- APIGW
  IAM -. controls .- L_API
  IAM -. controls .- S3_UP
  IAM -. controls .- L_PIPE
  SECRETS -. config .- L_API
  SECRETS -. config .- L_PIPE
```